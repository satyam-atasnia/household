<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lbm1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".l_view"></activity>
        <activity android:name=".c_view" />
        <activity android:name=".book" />
        <activity android:name=".l_s" />
        <activity android:name=".l_l" />
        <activity android:name=".c_s" />
        <activity android:name=".c_l" />
        <activity android:name=".l_login" />
        <activity android:name=".c_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>